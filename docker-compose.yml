services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - API_KEY=${API_KEY:?API_KEY is required. Set it in your .env file or pass it via -e API_KEY=...}
      - LITELLM_BASE_URL=${LITELLM_BASE_URL:-http://host.docker.internal:6655/litellm/v1}
